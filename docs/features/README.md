# Features

Features are product-focused verticals. Each feature contains its own `ui/`, `model/`, and `api/` pieces when needed. Keep features independent and prefer shared utilities for common code.
