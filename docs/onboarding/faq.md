# FAQ

Q: Which package manager should I use?

A: Use `npm` for CI compatibility. Bun is supported locally but CI uses Node.

Q: Where are global styles?

A: Look under `src/shared/styles` and `index.css`.
