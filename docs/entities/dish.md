# Dish entity

Represents a menu item.

Fields:

- `id: string`
- `name: string`
- `description?: string`
- `price: number`
- `category?: string`
- `imageUrl?: string`

Keep DTO -> domain mappers in `src/entities/dish` and validation in the `model` folder.
