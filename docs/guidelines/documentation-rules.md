# Documentation rules

- Keep docs in English.
- Keep docs short, link to source files when necessary (use relative workspace links).
- Update docs when making breaking changes — include ADR when appropriate.
- Use `docs/decisions` for long-lived architectural decisions.
